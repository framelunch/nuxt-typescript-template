export interface Person {
  id: number;
  firstName: string;
  lastName: string;
}

export type People = Person[];

export interface PersonEntity {
  id: number;
  first_name: string;
  last_name: string;
}
